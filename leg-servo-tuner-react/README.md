# leg-servo-tuner-react

**【注意】 このディレクトリ内のコードは現在更新されていません。上位互換のmotion-editor-reactが更新されています。**

サーボの角度を1つずつ調整するReactアプリです。

leg-servo-tunerをReactで実装し直しました。

## 起動方法

```bash
npx vite --host 0.0.0.0 --port 5173