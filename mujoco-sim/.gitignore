# dispatch runtime
mujoco_rl_sim/dispatch_data/

# worker secrets (local)
**/worker.config.local
**/coordinator.config.toml

# experiment dispatch artifacts
**/dispatch_summary.json

# RL checkpoints (existing convention)
mujoco_rl_sim/runs/

# Hydra 実行ログ（単発 run: outputs/、sweep: multirun/。正本は runs/.../.hydra/）
**/outputs/
**/multirun/

__pycache__/
*.py[cod]
.pytest_cache/
.venv/
wandb/
# Hydra の wandb config group（ランタイムの wandb/ ログは上記で引き続き無視）
!**/conf/wandb/
!**/conf/wandb/**
